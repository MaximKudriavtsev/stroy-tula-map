services:
  server:
    build:
      context: ./services/backend
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:4000:3000"
    restart: unless-stopped
  web:
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
  postgres:
    restart: unless-stopped
